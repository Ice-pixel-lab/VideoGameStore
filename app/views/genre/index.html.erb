<h2 class = "title is-2">Genre#index</h2>
<table class = "table">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
  <% @genres.each do |genre| %>
    <tr>
      <td><%= link_to genre.genre_name, genre %></td>
    </tr>
  <% end %>
  </tbody>
</table>
  <%= paginate @genres %>